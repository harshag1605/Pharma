# Pharmacy Frontend

React + Vite + Tailwind + Redux Toolkit/RTK Query dashboard for patients, pharmacists, and delivery agents.

## Run

```bash
npm install
npm run dev
```

The Vite dev server proxies `/api` to `http://localhost:8080`.
